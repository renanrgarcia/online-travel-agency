# archive

Pre-restructuring material, kept for reference rather than deleted — not part of the current spec.

- **`flight-ai-dossier.html`** / **`flight-ai-dossier.pt-BR.html`** — an onboarding-style pitch document
  ("What you need to know before you join the flights team") written before the eval-driven
  `docs/reference/` + `docs/features/` structure existed. Superseded by that structure as the source of
  truth — [`../README.md`](../README.md) doesn't reference these — but the framing and reasoning in
  here may still be useful raw material if the system grows new functionality later.

Nothing here is guaranteed to match the current implementation. Treat it as historical input, not
documentation — if it disagrees with `docs/reference/` or `docs/features/`, those win.
